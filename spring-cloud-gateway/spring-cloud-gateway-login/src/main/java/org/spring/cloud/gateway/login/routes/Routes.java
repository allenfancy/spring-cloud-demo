package org.spring.cloud.gateway.login.routes;

/**
 * 路由详解：
 * 一、传统路由配置
 *  所谓的传统路由配置方式是在不依赖于服务发现的机制情况下，通过配置文件中指定具体每个路由表达式与服务实例的映射关系来实现API网关对外部请求的路由。
 * 二、面向服务路由配置
 *
 * 三、服务路由的默认规则
 *  虽然Eureka与Zuul的整合。剩下只需要再维护请求路径的匹配表达式与服务名的映射关系即可。
 *
 */
public class Routes {
}
