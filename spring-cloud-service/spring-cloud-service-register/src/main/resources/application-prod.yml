# 端口信息
server:
  port: 9003
# 应用名称
spring:
  application:
    name: REGISTER-SERVICE
  profiles:
    active: prod
# 数据库的连接信息
# 日志级别
logging:
  level:
    key : info
  config: classpath:logback-spring.xml


eureka:
  client:
    serviceUrl:
      defaultZone:  http://eureka-server-02:7002/eureka/
